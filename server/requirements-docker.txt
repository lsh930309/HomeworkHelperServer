# ===========================
# Docker/VM 환경 전용 의존성
# ===========================
# 기본 requirements.txt를 먼저 설치 후 이 파일 사용
# 사용법: pip install -r requirements.txt && pip install -r requirements-docker.txt

# PostgreSQL 드라이버 (컴파일 필요, Docker에서만 설치)
psycopg2-binary>=2.9.9,<3.0.0

# 추가 프로덕션 의존성 (선택적)
# gunicorn>=21.0.0,<22.0.0  # 프로덕션 WSGI 서버
