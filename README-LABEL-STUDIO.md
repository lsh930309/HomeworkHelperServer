# 🏷️ Label Studio 빠른 시작 가이드

HomeworkHelper MVP 라벨링을 위한 Label Studio 사용 가이드입니다.

---

## 🚀 빠른 시작 (Windows)

### 1. Label Studio 시작

프로젝트 루트에서 **더블클릭**:
```
start-label-studio.bat
```

**실행 내용**:
- ✅ Docker 실행 확인
- ✅ Label Studio 컨테이너 시작
- ✅ 5초 후 브라우저 자동 열림

**접속 정보**:
- URL: http://localhost:8080
- 사용자명: `admin`
- 비밀번호: `homework-helper-2025`

---

### 2. Label Studio 중지

프로젝트 루트에서 **더블클릭**:
```
stop-label-studio.bat
```

**실행 내용**:
- ✅ Label Studio 컨테이너 중지
- ✅ 리소스 해제

---

### 3. 로그 확인 (문제 발생 시)

프로젝트 루트에서 **더블클릭**:
```
view-label-studio-logs.bat
```

**실행 내용**:
- ✅ 실시간 로그 출력
- ✅ 디버깅 정보 확인

---

### 4. 브라우저만 열기

이미 실행 중일 때 브라우저만 열기:
```
open-label-studio.bat
```

---

## 📋 라벨링 워크플로우

### Step 1: 프로젝트 생성

1. http://localhost:8080 접속
2. **Create Project** 클릭
3. 프로젝트 이름: `HomeworkHelper-UI-Detection`
4. **Next** 클릭

---

### Step 2: 데이터 임포트

**방법 1: 로컬 파일 업로드**
1. **Import** 탭 클릭
2. **Upload Files** 선택
3. `datasets/processed/` 폴더에서 이미지 선택
4. **Import** 클릭

**방법 2: Cloud Storage (권장)**
1. **Settings** → **Cloud Storage** → **Add Source Storage**
2. **Storage Type**: `Local files`
3. **Absolute local path**: `/label-studio/datasets/processed/`
4. **Sync Storage** 클릭

---

### Step 3: 라벨링 인터페이스 설정

1. **Settings** → **Labeling Interface** 클릭
2. **Code** 탭 선택
3. 아래 파일 내용 복사 & 붙여넣기:
   ```
   label-studio/config/labeling-template.xml
   ```
4. **Save** 클릭

**템플릿 포함 내용**:
- ✅ 89개 YOLO 클래스 (4개 게임)
- ✅ 카테고리별 색상 구분
- ✅ 한국어 라벨명 표시

---

### Step 4: 라벨링 시작

1. **Label** 탭으로 이동
2. 이미지 선택
3. **Bounding Box** 그리기:
   - 마우스로 드래그하여 UI 요소 선택
   - 올바른 클래스 선택 (예: `zzz_hud_main`)
   - 여러 개 추가 가능
4. **Submit** 클릭

**라벨링 팁**:
- ⚡ 단축키 사용: `R` (사각형), `1-9` (클래스 선택)
- 🔍 이미지 확대: 마우스 휠 또는 `+/-` 키
- ↔️ 다음 이미지: 화살표 키 또는 `Submit` 후 자동 이동
- 💾 자동 저장: Submit 즉시 저장됨

---

### Step 5: 라벨 검토 및 수정

1. **Label** 탭에서 완료된 항목 클릭
2. **Update** 모드로 수정
3. BBOX 조정 또는 클래스 변경
4. **Update** 클릭

---

### Step 6: 데이터 내보내기

**YOLO 형식 변환 준비**:

1. **Export** 버튼 클릭
2. **Export format**: `JSON` 선택
3. **Export** 클릭
4. 다운로드된 파일을 `label-studio/data/export/` 폴더에 저장

**YOLO 형식 변환** (향후 구현 예정):
```bash
python label-studio/scripts/export_to_yolo.py \
    --input label-studio/data/export/project-1.json \
    --output datasets/labeled/
```

---

## 📊 진행 상황 확인

### 라벨링 통계

Label Studio 대시보드에서 확인:
- **Total**: 전체 이미지 수
- **Labeled**: 라벨링 완료 수
- **Skipped**: 건너뛴 항목
- **Annotations**: 총 어노테이션 수

### 클래스별 분포

**방법 1: Label Studio UI**
- **Dashboard** → **Statistics** 탭

**방법 2: 스크립트** (향후 구현 예정)
```bash
python label-studio/scripts/stats.py
```

---

## 🔧 문제 해결

### ❌ "Docker가 실행되지 않았습니다"

**해결**:
1. Docker Desktop 실행
2. 트레이 아이콘에서 "Docker Desktop is running" 확인
3. `start-label-studio.bat` 재실행

---

### ❌ "포트 8080이 이미 사용 중"

**해결 1: 다른 포트 사용**
1. `label-studio/docker-compose.yml` 편집
2. `ports: - "8081:8080"` 으로 변경
3. 재실행

**해결 2: 기존 프로세스 종료**
```cmd
netstat -ano | findstr :8080
taskkill /PID <PID번호> /F
```

---

### ❌ "이미지가 로드되지 않음"

**해결**:
1. 이미지 경로 확인: `datasets/processed/` 폴더에 이미지 존재 확인
2. Docker 볼륨 마운트 확인: `docker-compose.yml` 볼륨 설정 확인
3. 컨테이너 재시작:
   ```cmd
   stop-label-studio.bat
   start-label-studio.bat
   ```

---

### ❌ "라벨 데이터가 사라짐"

**원인**: 컨테이너 삭제 시 데이터 손실

**해결**:
- ⚠️ `docker-compose down -v` 사용 금지 (볼륨 삭제)
- ✅ `stop-label-studio.bat` 사용 (데이터 보존)
- ✅ 정기적으로 Export 수행

**백업**:
```cmd
xcopy label-studio\data label-studio\data-backup\ /E /I /Y
```

---

## 📈 라벨링 목표

| 항목 | 목표 | 현재 |
|------|------|------|
| 총 이미지 | 1,000장 | - |
| 라벨링 완료 | 1,000장 (100%) | - |
| 클래스당 최소 | 50개 | - |
| 품질 검증 | 100% | - |

---

## 🎯 다음 단계

라벨링 완료 후:
1. ✅ YOLO 형식 변환
2. ✅ Train/Val/Test 분리 (80%/15%/5%)
3. ✅ 데이터셋 검증
4. ✅ YOLO 학습 시작 (Week 4)

---

## 📚 참고 자료

- [Label Studio 공식 문서](https://labelstud.io/guide/)
- [YOLO 데이터셋 포맷](https://docs.ultralytics.com/datasets/detect/)
- [label-studio/README.md](label-studio/README.md) - 상세 가이드
- [docs/mvp-roadmap.md](docs/mvp-roadmap.md) - 전체 계획

---

**작성자**: HomeworkHelper Dev Team
**최종 수정**: 2025-11-18
