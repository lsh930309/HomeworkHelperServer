# 🎬 비디오 타임라인 라벨링 워크플로우

HomeworkHelper MVP의 **효율적인 비디오 기반 라벨링** 전체 워크플로우

---

## 📊 워크플로우 개요

```
원본 비디오 (30분)
    ↓
[1] 비디오 세그멘테이션 (SSIM 기반)
    ↓
동적 배경 클립들 (10개 × 30초 = 5분)
  - UI 고정 + 배경 변화 구간 선택
  - YOLO 과적합 방지
    ↓
[2] Label Studio 비디오 타임라인 라벨링
    ↓
비디오 라벨 (1개 클립당 1-2분 작업)
    ↓
[3] YOLO 형식 변환
    ↓
학습 데이터셋 (10,000+ 프레임)
```

**효율**: 30분 원본 → 10분 라벨링 작업 → 10,000+ 학습 데이터 🚀

---

## Step 1: 비디오 세그멘테이션

### 목적
30분 원본 비디오에서 **동적 배경 구간만** 추출 (UI는 고정, 배경만 변함)

### 핵심 개념
- **선택**: 0.4 ≤ SSIM ≤ 0.8 (전투, 이동 - 배경 변화)
- **제외**: SSIM > 0.8 (메뉴, 일시정지 - 너무 정적, 과적합 위험)
- **제외**: SSIM < 0.3 (컷씬, 로딩 - 너무 혼란)

### 실행

```bash
python tools/video_segmenter.py \
    --input datasets/raw/zenless_gameplay_30min.mp4 \
    --output datasets/clips/zenless/ \
    --dynamic-low 0.4 \
    --dynamic-high 0.8 \
    --min-duration 5 \
    --max-segments 20
```

### 출력

```
datasets/clips/zenless/
├── segment_001.mp4  (30초 - 전투 화면, SSIM: 0.65)
├── segment_002.mp4  (45초 - 이동 중, SSIM: 0.58)
├── segment_003.mp4  (20초 - 탐험 화면, SSIM: 0.72)
├── ...
├── segment_010.mp4  (25초 - 전투 화면, SSIM: 0.61)
└── segments_metadata.json
```

**결과**:
- 30분 원본 → 5분 동적 배경 클립
- 메뉴 화면, 로딩 화면, 컷씬 등 자동 제거
- UI 고정 + 배경 변화 구간만 선택 → YOLO 과적합 방지

---

## Step 2: Label Studio 비디오 라벨링

### 목적
비디오 타임라인에서 UI 요소를 **한 번**만 라벨링

### 2-1. Label Studio 시작

```bash
start-label-studio.bat
```
→ http://localhost:8080

### 2-2. 프로젝트 생성

1. "Create Project" → `HomeworkHelper-UI-Detection`
2. Settings → Labeling Interface → Code
3. `label-studio/config/labeling-template.xml` 내용 붙여넣기

### 2-3. 클립 업로드

1. Import → Upload Files
2. `datasets/clips/zenless/*.mp4` 선택
3. Import

### 2-4. 비디오 타임라인 라벨링

**효율적인 방법**:

```
segment_001.mp4 (전투 화면, 30초)
┌─────────────────────────────────────┐
│ ▶ [00:00 ============> 00:30]       │
│ ┌─────────────────┐                 │
│ │ [게임 화면]     │                 │
│ │  ┌──┐ ← HUD     │                 │
│ │  └──┘           │                 │
│ └─────────────────┘                 │
│                                     │
│ [00:05] HUD 박스 그리기             │
│ → 클래스: zzz_hud_main              │
│ → 타임라인: [00:00 - 00:30]         │
│   (전체 30초 = 900 프레임 자동!)    │
│                                     │
│ [Submit] → 다음 클립                │
└─────────────────────────────────────┘
```

**라벨링 시간**:
- 클립 1개당 평균 1-2분
- 10개 클립 = 약 15분 작업
- **결과: 10,000+ 프레임 라벨링 효과!** 🎯

### 2-5. Export

1. Project → Export → JSON
2. `project-1.json` 다운로드
3. `label-studio/data/export/` 폴더에 저장

---

## Step 3: YOLO 형식 변환

### 목적
비디오 라벨 → 각 프레임 이미지 + YOLO txt 파일

### 실행

```bash
python label-studio/scripts/video_labels_to_yolo.py \
    --labels label-studio/data/export/project-1.json \
    --clips datasets/clips/zenless/ \
    --output datasets/labeled/ \
    --frame-interval 1
```

### 처리 과정

```
segment_001.mp4 (30초, 60fps = 1,800 프레임)
+ Label: HUD [00:00-00:30]
↓
1,800개 프레임 추출
├── zenless_frame_000000.jpg + .txt (YOLO 라벨)
├── zenless_frame_000001.jpg + .txt
├── ...
└── zenless_frame_001800.jpg + .txt
```

### 출력

```
datasets/labeled/
├── train/
│   ├── images/
│   │   ├── zenless_frame_000000.jpg
│   │   ├── zenless_frame_000001.jpg
│   │   └── ... (8,000장)
│   └── labels/
│       ├── zenless_frame_000000.txt  (YOLO 포맷)
│       └── ...
├── val/
│   ├── images/ (1,500장)
│   └── labels/
├── test/
│   ├── images/ (500장)
│   └── labels/
└── data.yaml  (YOLO 학습 설정)
```

**결과**:
- 총 10,000+ 프레임 생성
- 자동 Train/Val/Test 분할 (80%/15%/5%)
- YOLO 학습 즉시 가능

---

## 📈 효율성 비교

| 방법 | 라벨링 시간 | 생성 데이터 | 효율 |
|------|------------|-------------|------|
| **개별 이미지** | ~8시간 (500장) | 500장 | 1x |
| **비디오 타임라인** | ~15분 (10개 클립) | 10,000+장 | **50x** 🚀 |

**핵심 차이**:
- 이미지: 500번 박스 그리기
- 비디오: 10번 박스 그리기 (각 구간마다 수백 프레임 자동 적용)

---

## 🎯 UI 라벨링의 장점

게임 UI는 **위치가 고정**되어 있어 비디오 라벨링에 최적:

```
전투 화면 (30초 구간)
- HUD: 화면 상단 고정
- HP바: 좌측 상단 고정
- 스킬 버튼: 우측 하단 고정
→ 한 번만 라벨링 = 900 프레임 자동 적용!
```

**동적 객체** (사람, 차량 등)는 이 방법이 비효율적이지만,
**정적 UI**는 **최고의 효율**을 보입니다!

---

## 🔧 고급 활용

### 프레임 간격 조정

```bash
# 모든 프레임 (60fps → 모두 추출)
--frame-interval 1

# 절반만 (30fps 효과)
--frame-interval 2

# 1/3만 (20fps 효과)
--frame-interval 3
```

### 배치 처리

```bash
# 여러 게임 일괄 처리
for game in zenless honkai wuthering nikke; do
    python tools/video_segmenter.py \
        --input datasets/raw/${game}_gameplay.mp4 \
        --output datasets/clips/${game}/ \
        --dynamic-low 0.4 \
        --dynamic-high 0.8

    # Label Studio 라벨링 (수동)

    python label-studio/scripts/video_labels_to_yolo.py \
        --labels label-studio/data/export/${game}.json \
        --clips datasets/clips/${game}/ \
        --output datasets/labeled/${game}/
done
```

---

## 📚 다음 단계

라벨링 완료 후:

1. **데이터셋 검증**
   ```bash
   python tools/validate_labels.py --dataset datasets/labeled/
   ```

2. **YOLO 학습 시작**
   ```bash
   python training/train.py --data datasets/labeled/data.yaml
   ```

3. **모델 평가**
   ```bash
   python training/evaluate.py --weights models/best.pt
   ```

---

**작성자**: HomeworkHelper Dev Team
**최종 수정**: 2025-11-18
